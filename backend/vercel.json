{
  "version": 2,
  "builds": [
    {
      "source": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "source": "/(.*)",
      "destination": "server.js"
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "server.js" }]
}
